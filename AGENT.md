# AGENT.md

## 프로젝트 개요

- Next.js 기반 웹 애플리케이션으로 구현한다.
- JavaScript를 기본 언어로 사용한다.
- Tailwind CSS를 스타일링 도구로 사용한다.
- 정적 데이터 기반의 프론트엔드만 동작하며, 성향 테스트 질문지의 선택 결과를 단순 계산해 결과를 보여준다.
- 백엔드, 데이터베이스, 외부 API, 서버 상태 관리 기능은 추가하지 않는다.
- 새 코드는 기존 프로젝트 구조와 설정을 우선 따른다.

## 코드 작성·수정 기준

- 가독성, 유지보수성, 접근성을 우선한다.
- 작은 단위의 변경을 선호하며, 요청하지 않은 추상화나 대규모 리팩터링은 하지 않는다.
- 기존 컴포넌트, 유틸리티, 타입, 스타일 패턴을 먼저 확인하고 재사용한다.
- 컴포넌트는 역할을 작게 유지하고, 중복 로직은 공통화하되 한 곳에서만 사용되는 추상화는 만들지 않는다.
- 변수와 함수의 역할이 드러나는 이름을 사용하고, 필요한 경우 JSDoc으로 주요 데이터 구조와 함수 계약을 설명한다.
- 컴파일러와 린터의 경고를 오류처럼 다룬다.
- Tailwind CSS 유틸리티를 우선 사용하며, 중복되거나 의미 없는 클래스는 정리한다.
- 사용자 입력과 URL 파라미터 등 신뢰할 수 없는 값은 사용 전에 검증하고 안전하게 처리한다.
- 질문지와 선택지는 정적 데이터로 관리하고, 질문·선택지·점수 구조를 일관되게 유지한다.
- 결과 계산은 동일한 입력에 항상 동일한 결과를 반환하는 순수 로직으로 작성한다.
- 점수 계산 규칙과 결과 유형 매핑은 UI 컴포넌트와 분리해 관리한다.
- 선택지 미선택, 잘못된 선택 인덱스, 질문 데이터 누락 등 예상 가능한 입력 오류를 처리한다.
- 로딩, 오류, 빈 상태는 실제로 필요한 경우에만 포함하고, 반응형 레이아웃은 필요한 화면에 적용한다.
- 시맨틱 HTML, 키보드 탐색, 명확한 포커스 상태, 충분한 색상 대비를 지킨다.

## 테스트·검증

- 새 기능이나 버그 수정에는 가능한 범위에서 관련 테스트를 추가하거나 수정한다.
- 테스트는 결정적이어야 하며 구현 세부사항보다 사용자에게 보이는 동작을 검증한다.
- 변경 후 최소한 다음 검증을 수행한다.
  - JavaScript 문법 및 린트 검사
  - 린트
  - 관련 테스트
  - 필요한 경우 프로덕션 빌드
- 결과 계산과 관련된 테스트에는 다음 경우를 포함한다.
  - 모든 질문에 정상적으로 응답한 경우
  - 최소·최대 등 경계값 점수
  - 결과 유형 간 동점이 발생한 경우
  - 선택 누락 또는 잘못된 입력이 있는 경우
- 테스트를 실행할 수 없으면 이유와 실행하지 못한 항목을 작업 후 요약에 기록한다.

## 문서화

- 사용자에게 보이는 동작이나 실행 방법이 변경되면 관련 문서를 갱신한다.
- 문서에는 실제 사용 방법과 필요한 설정을 적고, 코드에 장황한 설명을 남기지 않는다.

## Git 사용 규칙

- 커밋, 푸시, 브랜치 생성·삭제는 사용자가 명시적으로 요청한 경우에만 수행한다.
- 커밋을 만들게 되면 하나의 논리적 변경만 포함하고 메시지는 변경 목적을 분명히 적는다.
- 기존의 사용자 변경사항이나 작업 중인 파일을 임의로 되돌리거나 덮어쓰지 않는다.
- 강제 푸시와 파괴적인 Git 명령은 사용하지 않는다.
- 인증 정보, API 키, 토큰, 개인정보를 커밋하지 않는다.
- 이 프로젝트에는 백엔드, 데이터베이스, `.env` 파일이 없으므로 이를 전제로 한 설정이나 의존성을 추가하지 않는다.

## 보안

- 비밀정보를 소스 코드, 로그, 테스트 픽스처, 문서에 노출하지 않는다.
- 외부 입력을 HTML이나 셸 명령 등에 안전하지 않게 삽입하지 않는다.
- SSL 검증 비활성화, 인증 우회, 보안 헤더 제거 등 보안 완화는 하지 않는다.
- 의존성 추가가 필요하면 보안·유지보수 비용을 먼저 검토하고 이유를 기록한다.

## 파일 변경 제한·프로젝트 구조

- 변경 범위는 요청을 수행하는 데 필요한 최소 파일로 제한한다.
- 루트 설정 파일과 `app/`, `pages/`, `components/`, `public/` 등은 실제 프로젝트에 존재하는 구조와 규칙에 따라 처리한다.
- 프론트엔드에서 사용하는 정적 데이터와 화면 상태는 컴포넌트 또는 적절한 클라이언트 모듈에서 관리한다.
- 디렉터리가 존재하지 않는다면 필요성이 확인된 경우에만 만든다.
- 무관한 파일의 포맷 변경이나 정리 작업을 함께 수행하지 않는다.

## 작업 커뮤니케이션

### 작업 전

- 요청을 이해한 뒤 변경 대상, 구현 방향, 검증 방법을 짧게 제시한다.
- 요구사항이 모호하거나 보안·데이터 손실 위험이 있으면 구현 전에 확인한다.
- 기존 코드와 관련 호출 경로를 먼저 확인하고, 예상되는 위험이나 가정을 알린다.

### 작업 중

- 한 번에 하나의 논리적 변경을 진행한다.
- 범위가 바뀌거나 새로운 위험이 발견되면 즉시 공유한다.

### 작업 후

- 변경한 파일과 핵심 변경사항을 요약한다.
- 실행한 테스트·검증과 결과를 기록한다.
- 적용한 가정, 남은 제한사항, 필요한 후속 작업을 명시한다.
